pipeline {
    agent any
    tools {
     maven 'maven 3'   
     jdk 'jdk1.8.0_191'
    }
    stages {
        stage('Build') {
            steps {
                bat 'mvn install'
               
            }
        
        
             post {
                  success{
                        junit 'target/surefire-reports/**/*.xml'
                        
                        }
                  }
        }
        
    }
}